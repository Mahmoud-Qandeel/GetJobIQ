# Search Queries for Job Scraper — Reference Redirect

**This is a reference pointer, not a working copy.**

The actual search-queries.md file (generated by `/setup` and used by `/scrape`) lives here:

📍 **`01-FRAMEWORK/.claude/skills/job-scraper/search-queries.md`**

This is the **single source of truth** for job search queries. It is:
- Generated by `/setup` when you configure your job search strategy
- Read by `/scrape` at each job search run
- The only copy that matters for actual search functionality

**Why this folder exists:** During initial setup, it was provided as a human-readable reference alongside the working copy. Today, the working copy in `01-FRAMEWORK/.claude/skills/job-scraper/` is self-documenting, so this reference is maintained as a pointer only to avoid drift.

**If you've edited this file directly:** Those changes will NOT be used by `/scrape`. To update your search queries, run `/setup` again, which will update the working copy in `01-FRAMEWORK/.claude/skills/job-scraper/`.
